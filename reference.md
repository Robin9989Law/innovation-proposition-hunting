# 创新命题方法论 — 详细参考

配合 [SKILL.md](SKILL.md)。需要完整字段、关闭模式、Gate 动作或阶段细节时读取本文件。

---

## 目录

- [1. 为什么找创新点会失控](#1-为什么找创新点会失控)
- [2. 连续研究簇与近邻分级](#2-连续研究簇s2)
- [3. 证明障碍、证据阶梯与正反张力](#3-证明障碍备忘录字段s4)
- [4. 以近邻为跳板](#4-以近邻为跳板最低要求)
- [5. 前沿追溯与搜索停止](#5-前沿追溯s3)
- [文献—观点—输出证据流水线](evidence-pipeline.md)
- [6. 反模式与分层创新审计](#6-不应再当创新的模式)
- [7. Gate 完整表](#7-gate-不合格动作完整表)
- [8–10. 目录、记录与文献字段](#8-建议目录结构命题存活后)
- [11–14. 成功判据、停止扩展与方法修订](#11-突破口成功判据)

---

## 0. 现行层级与成果类型合同

先冻结成果类型，再采用对应层级：

```text
DOCTORAL_DISSERTATION
  L1 → L2 → 三个有机主贡献 A/B/C → 各自 L3 → 支持工具

JOURNAL_ARTICLE
  L1 → L2 → 一个主贡献 M → 主 L3 + 从属后果/组件 → 支持工具
```

N0 只用于 L3。博士 L2 必须容纳三贡献；一般期刊 L2 默认只设一个主贡献。
旧版“L2=知识命题、L3=方法”的定义已废止。

### 0.1 Schema 3.0 双轴裁决

N 轴只回答候选与前沿的关系；V 轴回答准备写入稿件的精确 claim 是否成立。合法
状态为 `N0-1 | N0-2 | N0-3 | N0-4C` 与 `V0 | V1 | V2 | V3 | V4`。只有
N0-4C 才能进入 claim freeze；N0-4C 不能自动提升任何 V level。

| V level | 裁决对象 | 绑定证据 | 不合格动作 |
|---|---|---|---|
| V0 | 尚未冻结的主张集合 | 无 readiness 声明 | 建 claim inventory |
| V1 | 精确 claim inventory | 稿件 occurrence 一对一绑定、profile 已冻结 | 修 inventory，不做 proof/protocol 裁决 |
| V2 | form-sensitive G9 | theory audit 和/或 protocol audit + code audit 均通过 | 修具体 obligation/trace/contract |
| V3 | 当前 epoch 的精确 bundle | 不同 agent、PASS、相同 epoch/hash | 禁止计算或方向锁 |
| V4 | 计算后新 epoch 的精确 bundle | 不同 agent 再审、PASS、相同 epoch/hash | 禁止最终锁或投稿强主张 |

profile 路由是强制的：`THEORY` 运行 theory audit；`ALGORITHM` 同时运行 protocol
audit 与 code audit；`MIXED` 运行三者。改变 profile 不能用来逃避缺失产物。V2
只说明当前作者侧 obligation 可执行；V3/V4 还要求 reviewer 与所有
`author_agent_ids` 不同，并审阅规范哈希固定的精确 bundle。

### 0.2 G9 是 V2–V4 的绑定审计组

G9 的三项组件分别约束不同失效面：

- `theory audit`：逐 claim 核对 exact statement SHA-256、量词、前提、proof locator，
  并检查 minimal positive、nonzero nuisance、boundary/limit、premise removal；
- `protocol audit`：冻结 prediction/update unit、顺序、标签可得性、split/data role、
  test access、在线 chronology 和同预算 comparator contract；
- `code audit`：把稿件位置与 pseudocode symbol 绑定到可达实现 symbol、实现 hash、
  实际调用该 symbol 的 executable test 及 PASS output。

G9 在 V2 绑定作者侧 claim artifacts；V3 绑定第一次 independent audit；V4 绑定
post-compute 新 epoch 的 independent audit。任一 material change 都使相应 bundle
hash 过期，V3/V4 立即失效；“只重新阅读旧报告”不能恢复。

### 0.3 高风险主张语义

以下主张必须进入 inventory，而不能留在散文里：theorem/lemma/corollary 标题，
exact/universal/necessary/sufficient/bounded/guaranteed/first/online 声称，以及
strong/fair/matched-budget 比较。风险词只是 scanner 入口；独立 reviewer 审的是
完整 statement、量词、前提和上下文，不能只审命中的词。

## 1. 为什么“找创新点”会失控

十类风险：

1. **旧对象换名**（对象、信息集、结论类型未变）
2. **成熟模块拼接**（无新关键引理，可被审稿人还原）
3. **摘要层面空白**（limitation/future work 受设定限制；后续/相邻领域可能已处理）
4. **现实动机与理论对象脱节**（终点不清、依赖人工、无法接冻结数据或形式对象）
5. **方案先行、文献追杀**（每提一个方案才搜索一次，因此永远落在下一篇论文后面）
6. **摘要式判死**（题名或摘要同词即关闭，没有核验实际对象、基线和完成度）
7. **成果类型/版本/主题串线**（把期刊单主贡献套成博士三贡献，或混用不同版本、主题和贡献编号）
8. **层次代偿**（用方法新代替 L3 命题新，或用 L1/L2 的宽范围掩盖命题为空）
9. **全文浅读/结果口径漂移**（读了正文却未核对正式定义、实际条件、基线、分母和表格，把作者宣传句当实验事实）
10. **创新尺度过大或路径单一**（要求整个 L1 从未被触及，只承认空白型创新，或用“统一/系统/全面”代替最小非机械知识步）

目标不是尽快命名题目，而是避免把不成立的题目推进到定理、实验或投稿。

---

## 2. 连续研究簇

合格形式：

```text
论文 A：正式对象 + 第一个可解版本
    ↓
论文 B：放松/强化同一对象中一个关键条件
    ↓
论文 C：下界、反例、算法改进或新边界
    ↓
当年前沿：最强结论及尚存障碍
```

核心论文须有直接引用、清楚比较或对同一正式问题的连续改进。仅都谈“可靠性/协作/概率保证”不构成连续链。

连续性来自**同一正式对象与语义操作**（如保留 vs 擦除配置来源），不来自关键词重合。

### 2.1 近邻直接性分级

| 等级 | 判据 | 能否直接否定课题层 L1 |
|---|---|---|
| 课题直接占据 | 对象、核心问题、目标量、行动/结构、条件、结论类型和关键比较基线均对齐 | 可以，但仍须先上钻 |
| 直接方法近邻 | 使用相似测度/归因/优化工具，但目标问题不同 | 不可以；吸收方法后审计对应贡献域的 L3 |
| 理论前驱/同构 | 在另一对象上给出相同数学骨架或充分特例 | 不可以直接判死；检查是否机械归约 |
| 方法借鉴 | 可提供实验、证明或实现工具 | 不可以 |
| 关键词近邻 | 仅题名、摘要或术语相似 | 不可以 |

经验规则：`marginal contribution` 可能是训练信用、工作流内删节点归因或跨结构价值排序；`compatibility` 可能是 ROS 接口安全或生成模型信息利用；`heterogeneous` 可能只是模型池不同。词相同不决定研究对象。

### 2.2 Scope lock（防止版本和主题串线）

每轮开始必须固定 scope_lock.md；模板与字段语义已迁入
[templates.md](templates.md) §13（权威版本），此处不再复制。

---

## 3. 证明障碍备忘录字段

| 字段 | 问题 |
|---|---|
| 正式对象 | 随机变量、图、系统、实验或协议如何定义？ |
| 信息边界 | 谁知道什么、何时知道、可传递什么？ |
| 主结论 | 何种界、等价、算法或下界？ |
| 关键步骤 | 证明依靠哪条分解、凸性、独立性、充分性、单调性或可观测性？ |
| 绑定假设 | 去掉哪条后关键步骤不成立？（须为可检验数学条件） |
| 已知失败 | 原文反例、限制段、后续纠正 |
| 当前前沿 | 该障碍是否已被当年工作处理？ |

禁止把绑定假设写成“真实场景更复杂”“模型有依赖”等泛化描述。

### 3.1 文献深读的证据阶梯

| 证据级 | 可回答 | 不可回答 |
|---|---|---|
| E0 搜索结果/题名 | 是否值得打开 | 任何覆盖、新颖性或首次判断 |
| E1 摘要+导言 | 论文声称研究什么；初步碰撞程度 | 论文实际完成了多少、基线是否公平 |
| E2 问题定义+方法/定理+实验设置/基线+结果 | 实际对象、信息集、完成度、比较责任 | 证明机器的 maximal reach |
| E3 limitation/future work+反例 | 作者承认的适用边界和后续线索 | 自动生成创新点 |
| E4 证明/附录/构造 | 绑定假设、像、逆、边界、机械推出可能性 | 系统前沿新颖性（仍需前沿追溯，见 §5） |

覆盖裁决至少需要 E2；理论同构或机械推出裁决通常需要 E4。E1 只用于发现碰撞。原文 limitation 只有与正文证明机器或正反结果形成内生断层时才有价值。

### 3.1.2 出版资格轴（与 E0--E4 正交）

| 状态 | 判据 | 终局判死资格 |
|---|---|---|
| `PUBLISHED` | 正式期刊卷期/文章号、出版社 final/online-first 页面，或官方会议 proceedings 已发布 | 有；仍须满足 E2/E4 |
| `PUBLISHED_WITH_PREPRINT_ALIAS` | 同一 work 有正式出版入口，同时保留 arXiv 等入口 | 有；裁决引用正式入口 |
| `ACCEPTED_NOT_PUBLISHED` | 只有录用声明、海报页或 to-appear 信息，没有正式 proceedings/期刊入口 | 无 |
| `PREPRINT_ONLY` | arXiv、SSRN、bioRxiv、working paper、未正式出版的 technical report | 无 |
| `SUBMISSION_ONLY` | OpenReview 投稿页或匿名/未确认接收版本 | 无 |
| `FORMAL_NON_PEER_REVIEWED` | 学位论文、机构正式报告或其他未经过同行评审的正式材料 | 无 |
| `STATUS_UNVERIFIED` | 尚未核验出版状态 | 无 |

出版资格和全文证据是两道独立门：

```text
terminal negative verdict
  = formally published
  AND sufficient E2/E4 evidence
  AND correct layer/directness
  AND completed ascent/kill audit.
```

预印本即使达到 E4，也只能形成 `PREPRINT THREAT`。它可以阻止过早锁定、推动
版本链追踪或提示候选高度危险，但不能产生 `FAIL / REJECTED / EXHAUSTED /
CLOSED`。若机械判死需要多篇文献，每个必要前提都必须分别通过正式出版门。

同行评审状态另行登记：

| 状态 | 是否计入 100 篇阈值 |
|---|---|
| `PEER_REVIEWED_PUBLISHED` | 是 |
| `PEER_REVIEWED_ACCEPTED_NOT_PUBLISHED` | 否 |
| `NON_PEER_REVIEWED` | 否 |
| `PEER_REVIEW_STATUS_UNVERIFIED` | 否 |

不能仅凭 venue 字符串猜测同行评审状态；必须保存官方 proceedings、期刊页面
或出版社记录作为核验入口。

### 3.1.1 结果口径四联表（E2 必做）

| 项 | 必记内容 | 常见误读 |
|---|---|---|
| 正式定义 | 术语、estimand、公式、量词 | 用摘要中的宽泛说法替代正式对象 |
| 实际处理条件 | 哪个实验臂、是否 reveal、是否 matched budget、是否 representative config | 把一个条件下结果归到另一个条件 |
| 基线与分母 | Best Individual、oracle、average、homogeneous、单模型、成本归一化 | 分母变化后仍沿用同一结论名称 |
| 表格与作者总结 | 原始数值、复算值、正文结论、附录限制 | 作者总结比表格更强却不降格 |

核心差值、比例、relative gap 或 improvement 若可由表格复算，必须记录复算式。四联表存在矛盾时，不猜作者意图，采用所有证据共同支持的最弱陈述，并把冲突登记为证据边界。

### 3.2 正例—反例张力备忘录

不要只摘 future work。并排记录：

| 项 | 正面论文 A | 反例/失败论文 B | 严格特例论文 C |
|---|---|---|---|
| 共同推理桥 | | | |
| 成立制度 | | | |
| 失败制度 | | | |
| 未观测条件变量 | | | |
| 可否统一成充分/失效边界 | | | |

只有 A/B/C 操作同一推理桥，边界问题才是连续簇内生问题。若只是不同任务都报告不同性能，不足以立题。

---

## 4. “以近邻为跳板”最低要求

1. 准确复述对象、信息边界、目标量、主结论
2. 指出证明中真正承担作用的绑定假设
3. 给出假设失效时仍可定义/可观察/可形式化的对象
4. 说明新结论不能由近邻直接代入、取极限或拼接得到
5. 先构造最小反例或最小分离实例，再谈一般理论
6. **被近邻覆盖时，先上钻覆盖理论本身（Q1–Q6）再决定去留**：T 的证明机器是否有 T 未审查的结构承诺？T 的像/逆/边界/maximal reach/停止理由是否已被完整刻画？上钻产生 T 未回答的精确问题 → 以 T 为对象立新候选；上钻穷尽 → 才可关闭或收窄

覆盖判定顺序：

1. 近邻覆盖的是结论、证明外壳，还是元问题？
2. 仅外壳 → 吸收并取消原创资格
3. 覆盖自然必要性 → **上钻覆盖理论**：追问 T 的像/逆/边界/停止理由（Q1–Q6）；上钻产生 T 未回答的精确问题 → 以 T 为跳板立新候选；上钻穷尽 → 关闭
4. 覆盖元问题 → **上钻 T 的证明机器**：T 自身是否有未审查的结构承诺（Q1）？T 的关键引理能否多走一步（Q5）？有开口 → 以 T 为对象继续；无开口 → 关闭
5. 同时推出目标对象、量词、结构条件与结论 → **上钻推出机制本身**：归约/同构是否完整？其边界在哪？T 为何停在这里（Q6）？上钻无开口 → 关闭整条线

深化借口无效时必须关闭：只加场景/接口、converse 可由一般定理实例化、后果全来自标准外壳、与原连续链脱节。

**上钻与深化的区别**：深化是在 T 旁边找差异；上钻是进入 T 内部，让 T 的证明机器自身暴露出 T 没有回答的问题。上钻的产出不是"T 未处理 X"，而是"T 的证明机器在 Y 处有未审查的结构承诺，该承诺的像/逆/边界未被 T 刻画"。

---

## 5. 前沿追溯

完成 `PRIOR_CLAIM_DRAIN` 后，每轮不是直接从历史起点搜索。先完成动态近三年
窗口或核验同 scope、同年份的已验证快照，再回溯基础链：

```text
RECENT_FRONTIER_PASS：[current_year-2, current_year]
  → 注册并核验所有相关近邻
  → 下载重要近邻并提取观点
  → FOUNDATIONAL_BACKFILL：只补近期链指向的必要前驱
```

当前为 2026 年，第一步固定为 2024–2026。近三年窗口不完整时，不得用旧经典
文献或更多关键词替代，也不得进入命题碰撞。

```text
原始版 / 会议版 / 期刊版
  + 引用该工作的后续论文
  + 作者及合作者续作
  + 同义术语与上位领域检索
  ↓
追溯至当前年份
```

未完成前禁止使用“首次”“无人研究”“真正空白”。

### 5.0 可移植检索栈：学术数据库优先

不要依赖某个 agent 专属工具名。优先使用当前环境可调用的学术数据库、API 或
连接器；OpenAlex 可用时优先用于发现，而不是从通用网页搜索开始：

```text
学术数据库检索（主题/题名/作者/来源/年份）
  → canonical work 元数据（作者、摘要、主题、DOI、引用关系）
  → 官方出版社/期刊卷期/会议 proceedings（正式出版与同行评审核验）
  → 通用网页检索/抽取（正文、官方入口补漏、最新网页状态）
```

每轮检索至少保存：

- 原始数据库、查询式与过滤条件；
- 数据库 work id、DOI、题名、年份、来源和引用数；
- canonical work 与预印本/会议版/期刊版的版本关系；
- 正式出版核验 URL；
- 学术数据库未覆盖而改用网页补漏的理由。
- 动态近三年窗口、当前碰撞轮次以及该命中属于 `RECENT_FRONTIER_PASS` 还是
  `FOUNDATIONAL_BACKFILL`。

数据库返回记录是发现证据，不自动等于正式出版资格。终局负面裁决仍需官方
出版入口加 E2/E4 内容证据。API key 只能通过连接器密钥或本地环境变量注入，
不得出现在研究档案、注册表、技能文件、命令历史或版本控制中。

若当前 agent 没有任何可验证学术发现或全文访问能力，标记
`BLOCKED_CAPABILITY` 并停止相关状态；不得虚构工具、命中、DOI 或全文阅读。

### 5.1 搜索停止与转入深读

“不限制单次搜索条数”不等于无限横向扩张。出现以下状态时停止新增关键词，转入全文连续簇深读：

- 直接近邻集合在两轮同义检索中不再出现新的正式对象或关键机制；
- 新结果主要重复已有作者/版本/方法线；
- 已有正例、失败反例和至少一个严格特例，可提出同一推理桥；
- 新论文只增加应用场景，不能改变六元组或比较基线。

反之，若版本链、作者续作、当年引用或上位理论尚未追到，则不得因为“文献已经很多”提前停止。

### 5.1.1 100 篇同行评审文献自动综合锁

同一主题达到 100 篇**去重、实质相关、正式发表且经过同行评审**的 canonical
works 时，自动进入：

```text
SYNTHESIS_LOCK
new generic search = forbidden
chain construction = mandatory
collision unit = research chain, not isolated paper
```

阈值只计 `PEER_REVIEWED_PUBLISHED`。以下记录继续注册，但不计入 100：

- 预印本与 working paper；
- 仅录用但尚无正式 proceedings/卷期的版本；
- 投稿页、技术报告和学位论文；
- 正式出版或同行评审状态未核验的材料；
- 同一 work 的重复版本、镜像和 alias URL。

每个微型领域研究链至少包含：

1. 原始正式问题与最早可识别版本；
2. 条件放松、量词强化、方法或边界推进；
3. 正面结果与失败反例/纠正工作；
4. 当前最强同行评审正式出版结果；
5. 仍未闭合的结构责任；
6. 预印本威胁作为旁注，不承担终局链条节点。

命题碰撞先判断它落入哪条链，再对齐整条链的
\((O,I,A,T,C,R,B)\)。只有链内同行评审、正式出版且达到 E2/E4 的必要节点
可以支持终局判死。

### 5.1.2 例外重开协议

`SYNTHESIS_LOCK` 后仅在必要时重开，先登记：

```text
现有哪条链无法回答：
缺失的是新正式对象、正式出版锚点、版本状态还是必要理论前提：
为何不能由现有 100+ 篇同行评审文献归纳得到：
限定进入的新领域：
精确查询：
最多补齐什么：
停止条件：
补齐后回到哪条链：
```

允许理由仅限：

- 存活命题不可避免地进入现有链外的新正式对象；
- 终局裁决缺正式出版且同行评审的来源；
- 关键版本链/作者续作未核清；
- 覆盖理论上钻缺少必要的外部理论前提。

补齐缺口后立即关闭例外，不把新领域扩成第二轮无界检索。

### 5.2 从前沿结果推进一步

前沿饱和后，不再问“哪里完全没人做”，而按以下链条生成候选：

```text
K = 最近、最强且结果口径已核准的已知结果
U = K 内部尚未闭合的推理/边界/识别责任
Δ = 只解决 U 的最小知识增量
```

Δ 合格需同时满足：

1. 仍属于同一连续研究链；
2. 不能由 K 的变量替换、加约束、换基线或现成两篇拼接推出；
3. 能形成严格反例、边界、可识别对照、可证伪预测或新关键引理；
4. 失败时有明确关闭条件；
5. 填入冻结 L2 的有效贡献域（博士 A/B/C；期刊 M），且不把 L2 缩成单一 Δ。

“整个领域无人研究”不是合格前提，也通常无法证明。创新性论证应写成“已有 K；仍缺 U；本文完成 Δ；近邻不能机械推出 Δ”。

K→U→Δ 不只服务空白型创新：成熟前沿深挖时，U 是 maximal reach、边界或
瓶颈；成熟方法论证新问题时，U 是源理论/技术与新问题之间尚未成立的语义映射、
识别责任或证据桥。

---

## 6. 不应再当创新的模式

1. 从工程机制直接生题（shadow/canary/rollback/资格门等），无基线无法表达的可复核风险对象
2. 从外部验证/路由失败反推新论文（检测信号 ≠ 可认证净价值）
3. 以“更一般”掩盖标准覆盖（噪声/非凸/新名词往往进入另一成熟领域）
4. 把近邻 limitation 自动译成自己的贡献（“本论文未处理 X”≠“X 可发表空白”）
5. 用术语/接口差异躲避覆盖（DAG、tokenizer、top-k、轮次、损失、API、角色等非自动创新）

---

## 6.5 上钻反模式（以下不是真上钻，实质仍是逃避或收窄）

1. **换场景冒充上钻**：只把 T 用到新场景（新数据、新任务、新接口），不进入
   T 的证明机器。这不是 R2 深挖。它只有在通过新问题冻结、语义映射、假设核验
   和新领域知识门禁时，才可能另行成为 R3+F2；否则只是横向平移。
2. **limitation 翻译**：把 T 的 limitation/future work 段直接译成新贡献。"T 未处理 X" ≠ "X 是 T 内部的结构问题"。须说明 X 为何是 T 的证明机器自身暴露的、而非外部添加的。
3. **标准外壳上钻**：在 T 上面加一层泛化（噪声、非凸、随机、在线）而不追问 T 自身的像/逆。这往往进入另一成熟领域，不构成对 T 的上钻。
4. **术语重包装**：用 T 的术语重新描述旧对象，不产生 T 未回答的精确问题。
5. **过早收窄**：被 T 覆盖后立刻收窄为 P\T（"T 没覆盖的那一小块"），而不追问 T 为何能覆盖、T 的覆盖边界在哪、T 的像外还有什么。收窄是上钻穷尽后的兜底，不是第一反应。

真上钻的判据：产出是 T 的证明机器内部暴露的、T 自身未刻画的精确结构问题（像/逆/边界/maximal reach/停止理由），而非 T 旁边的差异。

## 6.6 分层创新审计（L1 → L2 → 成果型贡献架构 → L3）

### L1 研究工作

判断整条研究程序是否被直接占据。逐项回答对象、核心矛盾、总目标、动态轴、
关键基线、适用范围和连续知识链是否与近邻整体对齐。

产出：

`PASS AS RESEARCH PROGRAM / PARTIAL COLLISION / FAIL AS PROGRAM`

### L2 可行创新域

判断冻结 L1 内的微领域是否：

- 明确对象、信息、行动、变化、结果和反方；
- 足以完成 L1 的核心责任；
- 比 L1 更窄但宽于任何单一命题；
- 博士能划出三个非重叠且有依赖的贡献域；期刊能收敛为一个自足主贡献；
- 未被直接近邻以相同机制边界和联合结果整体完成；
- 在数据、计算与授权上可执行。

产出：

`PASS AS FEASIBLE MICRODOMAIN / NEEDS REBOUNDARY / FAIL AS DOMAIN`

L2 不接受 N0 评级。

### 贡献架构

博士分别检查 A/B/C 的研究单位、主要自变量、目标量、证据和退出边界。三项
至少三处不同，并存在“前项输出成为后项输入”的逻辑或递归依赖。

一般期刊只检查 M：是否有唯一主知识输出、是否能在目标篇幅内自足、所有定理/
实验/算法组件是否从属于 M，以及是否隐藏了第二条并列主线。

产出：

`PASS AS NON-OVERLAPPING ORGANIC ARCHITECTURE / PASS AS ONE-MAIN-CONTRIBUTION ARCHITECTURE / REPARTITION`

贡献架构或标题不是创新证据。

### L3 具体命题

判断每个贡献域内的答案是否形成不可机械推出的新知识。合格形式包括：

- 充分制度 + 失效制度；
- 必要性 + converse/sharpness；
- 排序保持 + 排序反转条件；
- 可识别 + 不可识别边界；
- 可实现像 + 严格不可实现像；
- 算法保证或可证伪实验预测。

若只是“再比较一次”“更多数据验证”“某指标相关”或“关系复杂”，L3 FAIL。
若近邻换变量、加标准约束或串联两篇即可得到，记 N0-1/2。

### 方法/技术支持

记录 estimand、测度、算法、实验设计和证明工具中哪些为标准吸收件，哪些承担
L3 的不可替代机制。方法新不能挽救 L3 被占据；标准方法也不妨碍真正的新 L3。

### 分层结论矩阵

| L1 | L2 | 贡献架构 | L3 | 解释 |
|---|---|---|---|---|
| FAIL | * | * | * | 整条研究程序被占据；下层不能救题 |
| PASS | FAIL | * | * | 保留 L1，在其内部换 L2 |
| PASS | PASS | FAIL | * | 重新划分贡献，不重开 L1/L2 |
| PASS | PASS | PASS | FAIL/N0-1/2 | 替换该贡献域内命题 |
| PASS | PASS | PASS | N0-3 | 封存候选，不启动昂贵验证 |
| PASS | PASS | PASS | N0-4C | 锁定该贡献的创新方向 |

博士 A/B/C 必须分别审计 L3；期刊只审计 M，并将其他结果降为后果或组件。禁止
以整篇成果的宽口号掩盖有效贡献为空。

## 6.7 创新路径与创新形式：正交双轴

### 路径轴：新知识从哪里产生

| 路径 | K 的角色 | 合格的 U→Δ | 典型失败 |
|---|---|---|---|
| R1 `GAP_OPENING` | 刻画相邻研究链和覆盖边界 | 填补对齐 O/I/A/T/C/R/B 后仍无人承担的关键知识责任 | “没搜到”、future work、换数据/接口 |
| R2 `DEPTH_EXTENSION` | 已成熟的理论/技术前沿 | 推进 maximal reach、像/逆、边界、瓶颈或性能前沿 | 普通调参、标准特例、只说更深更好 |
| R3 `NEW_PROBLEM_SUBSTANTIATION` | 成熟理论/技术 + 新问题已有事实 | 建立新的问题形式化、语义映射、识别条件、证据桥或结论 | 工具先行、只套流程、只换数据集 |

每个 L3 固定一个主路径。R1 不是默认优先级；R2/R3 不因已有成熟理论或技术而
自动降级。需要审计的是新增知识责任是否非机械，而不是工具是否首次出现。

### 形式轴：最终交付什么

| 形式 | 最低知识合同 | 专属门禁 |
|---|---|---|
| F1 `NEW_THEORY` | 新理论或数学命题 | 精确定义、条件/量词、命题、证明责任、见证/反例、理论近邻差异 |
| F2 `MATURE_THEORY_NEW_DOMAIN` | 成熟理论在新领域产生的新知识 | 源理论—新对象语义映射、假设核验、新领域结论、适用/失效边界 |
| F3 `NEW_ALGORITHM` | 新计算或决策规则 | 伪代码、复杂度/资源合同、机制、matched-budget 基线、消融、失败门 |
| F4 `ALGORITHM_DEEPENING` | 对既有算法的深入优化 | 原算法瓶颈、修改变量、收益机制、保护约束、同预算胜出、退出条件 |

一个候选可有次路径或次形式，但只按一个主路径和一个主形式审计，不得重复计数。
例如 R2+F1 是深化成熟理论得到新数学命题；R3+F2 是用成熟理论正确论证新领域
问题；R2+F4 是沿已知瓶颈深入改进算法。N0-4C 必须同时通过通用非机械性门、
主路径门和主形式门。

### R3 与 F2 的额外防伪

成熟理论/技术可以完全不新。要成立，至少有一项新责任落在：

- 新领域对象的可检验形式化；
- 源理论假设在新领域中的成立、修正或失效证明；
- 不能由源理论直接读出的新领域结论；
- 新的识别设计、测量桥或反事实证据；
- 新领域特有的适用/退出边界。

若把领域名替换回源领域后所有定义、推理和结论都不变，或只需运行现成软件并
报告性能，记为机械应用，不得给 N0-4C。

## 7. Gate 不合格动作（完整表）

| Gate | 强制问题 | 不合格时 |
|---|---|---|
| G-1 Scope lock | 成果类型、贡献合同、当前版本、层级、上层冻结项、贡献编号、允许/禁止对象与比较基线是否冻结？ | 成果类型错配、混入旧主题或静默改题 → 拆分/回退 |
| G0 范围 | 对象可否暂时脱离既有数据/旧信号独立成立？ | 只能依赖旧信号 → 关闭 |
| G1 连续簇 | 核心文献是否同一形式对象/操作？ | 仅关键词相似 → 换簇 |
| G1.1 近期窗口 | 是否先完成 current_year-2 .. current_year 的近邻检索？ | 未完成 → 禁止碰撞和历史扩展 |
| G1.2 真实与全文 | 文献身份是否核验；CRITICAL/IMPORTANT 是否下载并校验哈希/版本？ | 未完成 → 不得进入 E2/E4 或支持输出 |
| G1.3 观点注册 | 重要观点、结论和方法是否原子化注册且有 locator？ | 只有论文列表 → 不得综合或裁决 |
| G1.4 输出支持 | 每个输出结论是否绑定已核验观点 ID 并说明推理关系？ | 只有文献支持 → 删除/补证据 |
| G1.5 旧观点耗尽 | prior-round UNUSED 是否为 0？ | 非零 → 禁止新碰撞与新搜索 |
| G2 前沿 | 危险近邻是否追至当年？ | 未完成 → 不得立题 |
| G2.5 结果口径 | 正式定义、实际条件、基线/分母、表格和作者总结是否一致？核心指标是否复算？ | 未核对或发生未解释口径漂移 → 不得覆盖/判死/重开 |
| G3 语义断层 | 能否写出精确公式/量词/构造差异？ | 仅自然语言差异 → 关闭 |
| G3.5 一步增量 | 能否写出 K→U→Δ，且 Δ 不可由 K 机械推出？ | 只能提出宏大空白、更多场景或系统整合 → 不得命名候选 |
| G3.6 路径 | 是否固定 R1/R2/R3 主路径并通过其专属责任门？ | 只贴标签或路径不符 → 重分类/降级 |
| G3.7 形式 | 是否固定 F1/F2/F3/F4 主形式并交齐最低产物？ | 形式无知识责任或仅实现包装 → 降为组件 |
| G4 最小见证 | 有限反例/临界见证/最小支撑？ | 无法最小化 → 暂缓 |
| G5 必要性 | 不可由近邻机械推出的结构限制？ | 标准特例 → **上钻覆盖理论**：特例化边界是否已被 T 完整刻画？T 的像/逆/停止理由（Q2/Q3/Q6）是否有开口？未完整 → 以 T 为对象继续；已完整 → 关闭 |
| G6 精确性 | 是否尝试 converse/sharpness/严格反例？ | 仅单向观察 → **上钻**：对覆盖理论执行 Q2（像）Q3（逆）后再判定；上钻无开口 → 降级 |
| G7 外壳吸收 | 去掉标准工具后，问题、映射、结论或算法规则还剩什么原创？ | 为空 → **上钻**：覆盖理论自身是否有未审查的结构承诺（Q1）？其关键引理能否多走一步（Q5）？有 → 以 T 为对象立新候选；无 → 关闭 |
| G8 独立审计 | 异构术语与当年近邻能否推出核心？ | 能推出 → **上钻推出机制本身**：归约/同构是否完整？其边界在哪？无开口 → 关闭/重构 |
| G9 form audit | theory audit 的量词/前提/见证是否闭合；protocol audit 与 code audit 是否把稿件、时间线、实现、测试和公平预算绑定？ | 任一 profile 到期组件失败即保持 V1/V2；不得进入不同-agent V3 审计 |
| G10 原型 | 能否 PASS 且不足条件下 FAIL？ | 重写测试 |
| G11 成文 | 期刊 M 能否自足；博士 A/B/C 能否分别成章并共同成链？ | 重新排序或收敛贡献 |
| G12 分层创新 | L1/L2/成果型贡献架构/L3 是否逐项裁决且互不代偿？ | 缺一层 → 不得宣称核心创新 |

---

## 8. 建议目录结构（命题存活后）

```text
<microfield>/
├── workflow_state.json           # 机器执行权威；一次只允许一个 active_state
├── S0_entry_card.md
├── scope_lock.md
├── SUMMARY.md                    # 每次重大裁决更新；区分有效状态与历史轨迹
├── near_neighbor_registry.json
├── near_neighbor_url_ledger.csv
├── literature_claim_registry.json
├── output_claim_support.json
├── literature_archive/
├── forward_trace_to_YYYY.md
├── deep_read_collision_matrix.md # E0--E4 证据等级与实际完成度
├── heterogeneous_terminology_audit.md
├── problem_and_neighbor_matrix.md
├── contribution_architecture.md  # 博士 A/B/C；期刊 M
├── hierarchy_novelty_audit.md    # L1/L2/贡献架构/L3 分开裁决
├── innovation_route_form_audit.md
├── exact_theorem_package.md
├── independent_novelty_audit.md
├── restricted_converse_proof_audit.md
├── prototype.py
├── solver_validation_report.md
└── manuscripts/
    ├── primary_target/
    ├── fallback_target/
    └── submission_checklist.md
```

权威优先级（唯一权威链定义于 [SKILL.md](SKILL.md) §1，此处不复制）：

```text
用户/导师/开题等显式权威要求
  > workflow_state.json 当前机器状态
  > SKILL.md 强制协议
  > 资源文件（templates / evidence-pipeline / compute-funnel / 本文件）
  > 案例与中间检索笔记、任务书和早期种子
```

---

## 9. 每次推进的最小十组记录

1. Scope lock：成果类型、贡献合同、碰撞轮次、近三年窗口、版本、当前层级、上层冻结项、贡献编号、允许/禁止对象、比较基线
2. 近邻文献 JSON：真实身份、搜索阶段、重要性、下载与哈希
3. 重要观点 JSON：观点/结论/方法、locator、条件、证据级和使用状态
4. 输出支持 JSON：每条输出结论到观点 ID 的映射与推理关系
5. 对象卡：对象、信息、目标、条件、结论
6. 深读碰撞卡：E0--E4 证据、论文声称 vs 实际完成
7. 结果口径表：定义、条件、基线/分母、表格、复算、最弱准确陈述
8. 前沿一步卡：K→U→Δ、主创新路径、主创新形式与非机械性
9. 最强近邻卡、正反张力、杀伤记录与分层创新审计
10. 量词账本、贡献责任表与可失败原型

---

## 10. 文献记录最低字段

```text
论文标识、规范题名、作者、最终版本、年份、会议/期刊、出版状态、真实身份状态、
正式出版核验入口与日期、终局判死资格、canonical URL、
别名 URL、动态近三年窗口、搜索阶段、首次/最近出现日期与文件、命中查询、
重要性、本地全文路径、下载来源、版本匹配、SHA-256、研究对象、信息边界、
主结论、证明/算法关键步骤、实验结构与关键比较基线、绑定假设、
原文限制、后续工作、与当前问题的六元组关系、近邻直接性等级、
证据等级（E0--E4）及升级历史、已核验章节/页码、对 L1/L2/贡献/L3 的影响、
碰撞/吸收结论、当前裁决、归档状态。
```

文献记录之外，另按观点原子保存观点类型、准确释义、条件、证据级、页/节/段/
定理/表/图/算法 locator、使用状态和所支持的输出 ID。最终输出必须能回到原文。

### 10.1 强制四账本

每个研究主题维护四个互相校验但职责不同的文件：

1. `near_neighbor_registry.json`：一篇 canonical work 一条记录，保存语义字段、
   真实身份、搜索阶段、重要性、下载/哈希、证据等级和裁决历史；
2. `literature_claim_registry.json`：一个重要观点、结论、方法、假设或反例一条记录，
   保存 source work ID、准确释义、条件、locator、证据级和使用状态；
3. `output_claim_support.json`：一个输出结论一条记录，绑定观点 ID、反向观点、
   推理类型、推理说明、限制与完整追溯状态；
4. `near_neighbor_url_ledger.csv`：每个 URL 每次出现一行，保存来源文件、行号、
   canonical work id 与是否已注册，用于机械发现漏项。

不得用 URL 账本替代论文级去重，不得用论文 ID 替代观点 ID，也不得因论文级
去重删除入口 URL。完整 schema 见 [evidence-pipeline.md](evidence-pipeline.md)。

注册表另保存 `peer_review_status`、`peer_review_verification_url`、
`peer_reviewed_published_count`、`search_mode` 和首次达到 100 篇的日期。
达到阈值后，新增记录必须关联一个 `search_reopen_log` 例外条目。

### 10.2 命中即注册协议

每次普通检索、引用链追踪、作者续作检索或全文阅读后立即执行：

1. 先完成动态近三年窗口，过滤明显无关的网页噪声；
2. 对每个实质相关学术命中分配 canonical work id 并核验真实身份；
3. 新命中最低登记 E0；CRITICAL/IMPORTANT 下载全文；只有实际阅读相应部分才升级
   E2/E3/E4；
4. 原子化注册其重要观点、结论、方法、假设和反例；
5. 记录它碰撞的是 L1、L2、贡献 M/A/B/C、某个 L3、覆盖理论还是方法基线；
6. 记录当前效果：直接占据、同构/前驱、机械拼接部件、方法吸收、反例、
   边界约束或仅待筛；
7. 把每个输出结论绑定到观点 ID，并保存直接/综合/对照/推理关系；
8. 保存首次与最近出现来源、全部入口 URL 和本地近邻卡；
9. 刷新 URL 账本并运行文献、观点、输出和旧观点耗尽检查。

同时核验 `publication_status`。仅有 arXiv 或投稿页时标记
`PREPRINT_ONLY/SUBMISSION_ONLY`；发现正式卷期或 proceedings 后升级为
`PUBLISHED_WITH_PREPRINT_ALIAS`，保留预印本 URL，但终局裁决只引用正式
出版入口。

“所有搜过的近邻”包括尚未全文核验但实质相关的 E0/E1 命中；不包括明显无关
的搜索噪声。未能解析题名或版本时仍先登记占位记录，标记
`METADATA_UNRESOLVED`，不得丢弃。

### 10.3 证据与裁决不可逆覆盖

- 证据升级采用追加历史，不删除旧 evidence event。
- 新版本出现时链接 `supersedes` / `is_version_of`；只有确认同一 work 后合并。
- 关闭、复活、降级或影响层级改变均追加 decision event，保留日期、来源文件
  与理由。
- 本地“存档”至少包含元数据、原始 URL、检索来源和裁决记录；全文仅在合法
  可访问且任务需要时保存。不得把只有 URL 的记录写成“全文已归档”。
- 出版状态升级追加历史；不得因为题名相同就猜测预印本已正式出版。

### 10.4 裁决前完整性门禁

在任何层级裁决前同时满足：

- 动态近三年窗口已完成；
- 所有文献真实身份已核验；CRITICAL/IMPORTANT 全文已归档并核对 SHA-256；
- 裁决稿、深读卡和结果表出现的学术 URL 均映射到 registry id；
- 每个输出结论至少绑定一个已全文核验、带 locator 的观点 ID；
- 每个观点可回到 canonical work、本地全文与官方身份入口；
- 所有 prior-round 观点均为 USED 或 EXCLUDED_WITH_REASON，UNUSED=0；
- 每个实质相关的当轮搜索命中已登记或有明确排除理由；
- E2/E4 覆盖结论含已核验位置；
- 每项 `FAIL/REJECTED/EXHAUSTED/CLOSED` 的决定性文献均为
  `PUBLISHED` 或 `PUBLISHED_WITH_PREPRINT_ALIAS`；
- 机械拼接中的每个必要前提均通过出版资格门；
- 仅预印本碰撞写成 `PREPRINT THREAT / OPEN`，没有伪装成终局关闭；
- canonical id 无重复，alias URL 无悬空；
- `unregistered_relevant_hits = 0`。
- 若 `peer_reviewed_published_count >= 100`，搜索模式为
  `SYNTHESIS_LOCK`；阈值后的新文献均有例外重开记录；
- 已生成微型领域研究链和链条碰撞矩阵，未继续用孤立论文判定命题。

注册表不完整只能阻止裁决，不能被解释为“没有近邻”。

---

## 11. 突破口成功判据

至少同时满足：

- [ ] 单一连续微领域，非跨域拼接
- [ ] 危险近邻已追至当年
- [ ] 能用公式或量词写出真实语义断层
- [ ] 有最小有限见证或反例
- [ ] 至少一个不可由近邻机械推出的关键引理
- [ ] 已追问并尽可能完成 converse/sharpness
- [ ] **被覆盖时已对覆盖理论执行上钻六问（Q1–Q6），突破口来自 T 内部未审查的结构承诺而非 T 旁边的差异**
- [ ] 能给出严格不可实现边界
- [ ] 标准算法/组合理论外壳已降级吸收
- [ ] 独立新颖性审计未发现同构覆盖
- [ ] 逐量词 proof audit 未发现不可修复反例
- [ ] 原型正例通过、不足条件能失败
- [ ] 能在目标成果范围内形成自足叙事（博士按章成链；期刊 M 自足）
- [ ] 成果类型和贡献合同已冻结；博士 A/B/C 或期刊 M 的架构与 L3 已分别裁决
- [ ] 主创新路径 R1/R2/R3 与主创新形式 F1/F2/F3/F4 已固定并通过各自门禁
- [ ] 核心创新不依赖层次、路径或形式标签代偿
- [ ] 覆盖裁决来自 E2/E4 全文证据，不来自标题、摘要或 future work 翻译
- [ ] 核心结果已通过定义—条件—基线/分母—表格四联表审计，可复算指标已复算
- [ ] 候选以最近可靠结果 K 为锚，只声明一个 K 不能机械推出的最小知识增量 Δ
- [ ] 所有终局判死均由正式出版且达到 E2/E4 的文献承担；预印本只登记为威胁
- [ ] 近三年窗口先于历史回溯完成，所有文献身份真实可验证
- [ ] CRITICAL/IMPORTANT 全文已下载、版本一致且 SHA-256 可复核
- [ ] 每个输出结论由已定位的重要观点 ID 支持，而非只列文献
- [ ] 全部引用链 OC→LC→W→locator→全文→官方入口通过验证
- [ ] 新碰撞前 prior-round UNUSED 观点为 0

---

## 12. 必须停止扩展（主定理闭合后）

- 会改变诊断/语义定义的 fairness → 冻结
- 异构投影改变共同接口 → 冻结
- 非确定性并合改变临界见证语义 → 冻结
- 运行时模式切换 ≠ 静态分划 → 冻结
- 学习组件有限状态抽象有效性 → 另一验证问题，冻结

当前活跃贡献只需完成一个最小、精确、自足的闭环；期刊以 M 为整稿主闭环，
博士按 A/B/C 分别闭环后再检查有机链。

---

## 13. 成功后仍须保留的否定纪律

- 不用“本轮未检到”替代系统新颖性证据
- 不用有限原型替代一般证明
- 不用主定理后果包装成第二个独立贡献
- 不用成果主线桥梁扩大形式定理适用范围
- 不把“手稿完成”写成“贡献已被共同体确认”
- 新近邻出现时仍须追溯版本链到当年最新状态

---

## 14. 对初版方法的二十项修订（摘要）

1. 关闭优先，但不能关闭过早（精确到主张层）；**未经上钻的关闭是逃避**
2. 连续簇核心是同一语义操作
3. 优先寻找"像是什么"（image characterization）；**被覆盖时上钻覆盖理论的像/逆/边界，而非收窄为补集**
4. 必要性后立即追问 converse
5. 区分贡献核心与证明外壳
6. 编码/表示口径属于定理
7. 成文门禁应提前进入流程（N0-4C 后即检查期刊 M 自足性或博士章节衔接）
8. 事实先于方案：未完成连续簇实际完成度表，不得连续生成候选
9. 碰撞发现与覆盖裁决分离：E1 发现，E2/E4 裁决
10. L1、L2、成果型贡献架构、L3 与方法支持互不代偿
11. 从正例—反例共同推理桥生成边界问题，优先于 limitation 翻译
12. 冻结版本/主题/上层状态/贡献编号和比较基线，禁止探索中静默改题
13. “阅读全文”后仍须做结果口径四联表与核心指标复算；摘要、正文总结不得覆盖表格事实
14. 默认只在冻结贡献域内从最近可靠结果 K 推进一步 Δ；不再要求整个 L1/L2 是无人触及的大陆，也不以“系统/全面”替代最小非机械 L3 责任
15. 先冻结成果类型：博士 L2 必须容纳 A/B/C 三贡献，一般期刊 L2 默认一个主贡献 M
16. 把创新路径（R1 空白、R2 深挖、R3 新问题论证）与成果形式（F1 理论、F2 新领域应用、F3 新算法、F4 算法深改）分开审计
17. 每轮先耗尽旧观点，再完成动态近三年近邻检索（2026 年为 2024–2026），
    最后才回溯历史基础文献
18. 文献、重要观点和输出结论分别注册；论文存在不等于具体观点支持
19. CRITICAL/IMPORTANT 全文必须合法下载、核对版本并保存 SHA-256；所有引用形成完整追溯链
20. 开启新碰撞前先耗尽旧观点；prior-round UNUSED 非零时禁止继续搜新或立新候选

细节与案例机制见 [case-lessons.md](case-lessons.md)。
