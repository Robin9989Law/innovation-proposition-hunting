# L2 TRIAGE：K 集合选拔记录

本文件是 `k_set_selected` 门的产物（schema 3.0，L2_TRIAGE 状态）。

## 选拔方式

以元数据 + E1 摘要 + 少量试读（≤12 篇全文预算）对注册近邻进行选拔，
只有进入 K 集合的工作才在 L3_EVIDENCE 段跑全文归档与原子观点提取。

## K 集合

本 fixture 为最小合法项目：K 集合 = 碰撞综合实际引用的核心近邻，
清单与理由见 `collision-round1.md` 的 K 链（本 fixture 从 2.0 迁移，
选拔记录为迁移后补写，原始证据见各注册表 locator）。
